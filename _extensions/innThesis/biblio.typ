$-- innThesis places the bibliography itself (see innThesis.lua), so that the
$-- reference list lands where the author put the references chapter in
$-- _quarto.yml -- normally before the appendices -- rather than at the very end
$-- of the book, which is where Quarto's default partial would put it.
